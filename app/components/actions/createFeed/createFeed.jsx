/*
'use client';
import React from 'react';

'use server';
export async function createFeedLeed(postData) {
    let postData = {
      header: post.get('header'),
    }

    try {
      const res = await fetch('/akhundelar')
      if (!res.ok) {
        throw new Error('Fetch failed.');
      }
      return res.json();
    } catch (e) {
      console.err('Error.');
      return 'yo';
    }

  return (
    <>
      {rawPostData.content}
    </>
  );
  */ 
